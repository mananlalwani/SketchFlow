# Local development defaults. Do not put credentials in this file.
# Server configuration is documented in apps/server/.env.example.
# Browser-safe Vite variables are documented in apps/client/.env.example.

NODE_ENV=development
PORT=3000
HOST=0.0.0.0
