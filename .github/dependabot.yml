version: 2
updates:
  # Routine npm updates are intentionally handled in reviewed maintenance
  # batches. Dependabot security updates remain available independently.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: docker
    directory: /
    schedule:
      interval: weekly
